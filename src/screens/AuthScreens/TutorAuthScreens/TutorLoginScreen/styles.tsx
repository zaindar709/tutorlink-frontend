import { StyleSheet } from 'react-native';

/** Same tokens as Student Login — keep tutor/student auth visuals aligned. */
export { createStyles } from '../../StudentAuthScreens/StudentLogin/styles';
